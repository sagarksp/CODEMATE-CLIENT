import { useSelector } from "react-redux"

export const Feed =()=>{
    const user = useSelector((store)=>store.user)
    return(
        <>
        <p>Feed
            
        </p>
        
        </>
    )

} 